# Knowledgebase

This directory contains machine-readable metadata for CAPS tutoring content.

- `catalog.json`: canonical index of CAPS subject markdown files.
- `subjects/`: reserved for future normalized subject bundles (non-destructive migration path).

The original educational markdown files remain in the repository root and are referenced by `catalog.json`.
